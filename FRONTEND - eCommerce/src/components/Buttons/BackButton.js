import React from 'react';

const BackButton = ({origin}) => {
    return ( 
        <button className="back-button">
            <span className="back-button-icon"><i class="fas fa-angle-left"></i></span>
        </button>
     );
}
 
export default BackButton;